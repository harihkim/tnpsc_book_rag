# Base URL of the TNPSC Book RAG backend.
# Per api_spec.md §3.1 the frontend MUST NOT hardcode a dev host — set it here.
# Leave empty to run fully on mocks (no backend required).
# PUBLIC_API_BASE_URL=http://localhost:8000
