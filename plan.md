# TNPSC Book RAG — MVP Implementation Roadmap

## 1. Purpose

Build an English-only RAG backend for Tamil Nadu State Board textbooks for standards 6 through 10. The system must extract structured content from digital PDFs, preserve page and image provenance, provide standalone semantic search, and optionally generate LLM explanations from retrieved textbook evidence.

The implementation order is deliberately evidence-first:

1. Establish the storage and data foundations.
2. Extract trustworthy, page-linked content.
3. Prove retrieval quality without an LLM.
4. Assemble deterministic, inspectable context.
5. Add grounded and supplemented LLM answers.
6. Harden the complete MVP.

An LLM must not compensate for weak extraction or retrieval. Each phase has an exit gate that must pass before the next phase begins.

## 2. MVP Scope

### Included

- English-medium textbooks only.
- Standards 6 through 10.
- Digital PDFs with an accessible text layer.
- Book metadata: title, standard, subject, edition/academic year, publisher, and language.
- Original PDF preservation.
- Page-level text and provenance.
- Printed page label and PDF page index as separate fields.
- Extraction and preservation of figures, maps, diagrams, photographs, and captions.
- Structure-aware, page-aware chunking.
- Local English embedding generation.
- PostgreSQL with pgvector.
- A semantic-search API that does not require an LLM.
- An LLM answer API with two modes:
  - `textbook_only`
  - `textbook_plus_general`
- Optional SSE answer streaming with a final validated structured answer.
- Free hosted LLM providers accessed through PydanticAI v2.
- Typed and validated answer outputs.
- Structured JSON logging and OpenTelemetry instrumentation.
- Ruff, `ty`, Pyrefly, pytest, and retrieval/answer evaluation gates.
- Docker-based local development.

### Explicitly deferred

- Tamil extraction, search, or answer generation.
- Scanned-PDF OCR.
- Visual question answering or multimodal embeddings.
- Automatic description of uncaptioned images.
- Hybrid lexical/vector retrieval.
- Reranking models.
- User accounts, subscriptions, payments, and multi-tenant authorization.
- Frontend implementation.
- Distributed workers, Redis, Kubernetes, or production autoscaling.
- A separate LiteLLM proxy or other shared model gateway.
- Fine-tuning embedding or generation models.

## 3. Guiding Decisions

### 3.1 Extraction is the source of truth

- The original PDF is immutable source material.
- Docling output is preserved as a versioned extraction artifact.
- Derived pages, chunks, assets, and embeddings can be recreated.
- Raw extracted text and normalized retrieval text are stored separately.
- No OCR is forced for the MVP. A document without a usable text layer fails validation with a clear reason.

### 3.2 Provenance is mandatory

- Every searchable chunk must resolve to a document, book, and source page.
- PDF page index and printed page label must never be conflated.
- Bounding boxes are preserved when Docling supplies them.
- LLM citations are generated from backend evidence records, not free-form page numbers produced by the model.

### 3.3 Search remains local and independent

- Document and query embeddings are generated locally.
- Semantic search continues to work if all hosted LLM providers are unavailable.
- Retrieval scores are ranking signals, not confidence probabilities.

### 3.4 Free LLM providers are replaceable dependencies

- PydanticAI v2 provides the typed generation interface and provider fallback behavior.
- Provider names, model names, and fallback order are configuration, not business logic.
- The application must tolerate rate limits, model removal, provider outages, and changed quotas.
- No provider key is stored in the database or committed to source control.
- Only one routing/retry layer is used in the MVP; LiteLLM is not stacked beneath PydanticAI.

### 3.5 Laptop-aware operation

- Default ingestion concurrency is one document.
- Temporary files are removed after a successful ingestion.
- Page preview images are not generated by default.
- Only extracted document images are preserved.
- Local LLM inference is not part of the MVP.

## 4. Target Architecture

### Runtime components

1. **FastAPI application**
   - Book and document management endpoints.
   - Ingestion status endpoints.
   - Semantic-search endpoint.
   - Answer-generation endpoint.
   - Local query embedding.
   - PydanticAI v2 generation and structured-output validation.

2. **Ingestion worker**
   - Claims persistent jobs from PostgreSQL.
   - Runs Docling extraction.
   - Writes pages, assets, and chunks.
   - Generates embeddings in batches.
   - Performs cleanup and updates ingestion state.

3. **PostgreSQL with pgvector**
   - Catalog and document versions.
   - Ingestion state.
   - Page/chunk/image provenance.
   - Embeddings and semantic retrieval.
   - Retrieval and generation audit records.

4. **Shared artifact storage**
   - Local Docker volume for the MVP.
   - Original PDFs, Docling JSON, and extracted images.
   - Accessed through a storage abstraction so MinIO/S3 can be added later.

### Backend package and dependency boundaries

The backend contains two internal Python packages in one distribution and one `uv` project:

- `tnpsc_book_rag` is the application shell. It owns FastAPI, worker startup, settings,
  persistence, provider configuration, observability, and infrastructure adapters.
- `tnpsc_rag` is the domain-specific RAG core. It owns provider-neutral search, evidence,
  context, answer, and citation contracts plus orchestration policies.

`tnpsc_rag` is internal to this backend, not a separately published or generic RAG framework.
It retains TNPSC textbook concepts such as standard, subject, book/document identity, page
provenance, answer mode, and evidence citations.

Dependency direction is one-way:

`FastAPI/worker -> application services and adapters -> tnpsc_rag contracts`

The core must not import FastAPI, SQLAlchemy, pgvector clients, PydanticAI, or provider SDKs.
Instead, it defines small protocols implemented by infrastructure adapters. The standalone
search path uses retrieval only; the answer path composes retrieval, context assembly, and
generation without making search depend on an LLM provider.

### Initial deployment boundary

- One Compose project.
- One API container.
- One worker container.
- One PostgreSQL container with pgvector.
- One shared artifact volume.
- No Redis, LiteLLM proxy, or separate model-gateway container initially.

## 5. Data Model

The exact SQL is defined during implementation, but the following ownership boundaries should remain stable.

### `books`

Represents a conceptual textbook.

- Stable ID.
- Title.
- Standard: 6–10.
- Subject.
- Language: English for the MVP.
- Publisher.
- Optional board/catalog identifier.
- Created and updated timestamps.

The repository projects document count, active document, latest document/state, and derived catalog
status for the public Book resource. These availability fields are not independently mutable book
attributes and must remain consistent with document activation.

### `book_documents`

Represents a particular PDF/edition of a book.

- Book ID.
- Edition or academic year.
- Source filename and media type.
- Artifact location.
- SHA-256 checksum.
- File size and page count.
- Extraction/activation state.
- Active-version flag or activation timestamp.
- Created and updated timestamps.

A replacement edition creates a new document row. Existing citations remain tied to the edition from which they were produced.

### `pages`

- Document ID.
- Zero-based PDF page index, exposed with an explicit human-facing fallback label in the API.
- Printed page label, nullable and manually correctable.
- Width and height.
- Raw extracted page text.
- Normalized page text.
- Extraction warnings.

The API should expose an unambiguous human-facing page number while retaining the underlying PDF index.

### `assets`

- Page ID.
- Asset type: image, diagram, map, photograph, figure, or unknown.
- Artifact location.
- MIME type.
- SHA-256 checksum.
- Intrinsic pixel width and height when the preserved asset has a raster size.
- Caption, when present.
- Alt text, alt-text source, and decorative/informative status for explicit accessibility handling.
- Bounding box and coordinate origin.
- Optional canonical thumbnail artifact location plus intrinsic thumbnail dimensions.
- Docling reference/provenance metadata.

Binary image data is never stored directly in PostgreSQL.

### `chunks`

- Page ID.
- Document ID for efficient filtering.
- Stable sequence number within the document.
- Display text.
- Embedding text with book/chapter/section context.
- Chapter and section path.
- Content type: prose, heading, table, caption, or mixed.
- Token count.
- Content checksum.
- Source bounding boxes or provenance payload.
- Previous/next chunk relationship derivable from sequence.

Chunks should ordinarily remain within one page. Exceptional cross-page content must use an explicit page-link table rather than hiding the additional provenance.

### `chunk_pages`

Created if cross-page chunks are permitted.

- Chunk ID.
- Page ID.
- Span order.
- Optional character span.

### `chunk_embeddings`

- Chunk ID.
- Embedding model identifier and revision.
- Vector dimension.
- Content checksum used to create the embedding.
- Vector value.
- Created timestamp.

Only one active embedding model is required for the MVP. Keeping embeddings separate from chunks makes re-embedding auditable and avoids rewriting extracted content.

### `ingestion_runs`

- Document ID.
- Status and current stage.
- Worker identity.
- Docling version and configuration fingerprint.
- Chunker version and configuration fingerprint.
- Embedding model version.
- Started/completed timestamps.
- Retry count.
- Structured warning/error details.

### `answer_runs`

- Query text.
- Answer mode.
- Retrieval parameters.
- Retrieved chunk IDs and scores, preferably through a child table.
- Provider and actual model used.
- Prompt version.
- Token usage and latency when available.
- Generation status/error category.
- Validated structured answer or sanitized public problem for recovery.
- Request ID plus created/completed timestamps and retention expiry.

Do not store secrets or full provider credentials in audit records.

## 6. Lifecycle and State Machines

### Document lifecycle

`uploaded -> queued -> extracting -> chunking -> embedding -> ready`

Failure at any processing step moves the document to `failed` with the last successful stage and an actionable error. Retry creates or resumes an ingestion run without silently duplicating active content.

### Activation rule

- A document is invisible to search until it reaches `ready`.
- Activation occurs transactionally after extraction and embedding complete.
- If a new edition fails ingestion, the previously active edition remains searchable.

### Idempotency

- Source checksum prevents accidental duplicate uploads.
- Page, chunk, and asset writes are tied to an ingestion run.
- Reprocessing uses versioned configuration fingerprints.
- Re-running the same input and configuration should produce the same logical chunks and checksums.

## 7. Phase 0 — Foundation and Contracts

**Status: complete (2026-07-16).**

### Objective

Create the minimum reliable skeleton required by extraction without implementing search or LLM behavior.

### Current implementation checkpoint

- Completed the FastAPI application skeleton, typed settings, internal RAG contracts, catalog and
  ingestion lifecycle values, guarded structured logging, OpenTelemetry boundaries, and async
  database lifecycle.
- Completed package-owned Alembic revisions for pgvector and the initial catalog, ingestion,
  page, asset, chunk, cross-page provenance, and versioned embedding schema.
- The schema uses zero-based `pdf_page_index` values, activation timestamps with one active
  document per book, one queued/running ingestion attempt per document, and 384-dimensional
  vectors without an HNSW index.
- Completed portable server-owned artifact keys, an async provider-neutral storage protocol, and
  an atomic local adapter with traversal/symlink protection, streamed SHA-256 verification,
  immutable idempotent writes, byte limits, explicit deletion, and readiness integration.
- Finalized the frontend-facing API v1 resource, lifecycle, error, optional count, idempotency,
  search, semantic answer-block, optional SSE, recovery, accessibility, thumbnail, and
  asset-serving contract in `api_spec.md`. The complete OpenAPI 3.1 contract in
  `openapi.v1.yaml` is the source for frontend types and mocks; both contracts mark routes as
  implemented or planned so planned mocks cannot be mistaken for live backend behavior.
- Completed immutable catalog entities, the application-facing catalog repository protocol, the
  SQLAlchemy adapter for books and source documents, and a database-owned async transaction
  boundary. Real PostgreSQL tests prove normal exit commits, exceptional exit rolls back, and
  repository methods do not own transaction completion.
- Completed public capabilities, exact configured-origin CORS behavior, active-ready catalog
  filters, book listing/detail/create operations, derived availability projections, optional exact
  counts, and filter-bound bidirectional keyset cursors. Versioned errors use the frozen safe
  Problem Details shape, while private artifact keys stay outside document responses.
- Completed globally unique durable idempotency records with advisory transaction locking,
  canonical request fingerprints, exact response replay, conflict detection, and a minimum 24-hour
  retention contract.
- Completed bounded PDF acceptance with media/signature checks, streamed SHA-256 calculation,
  content-addressed immutable storage, duplicate-source detection, and atomic queued document plus
  ingestion-run creation.
- Completed the non-root Python 3.13 runtime image and Compose topology for PostgreSQL/pgvector,
  one-shot migrations, API, worker, and shared artifacts. API readiness and the worker's atomic,
  freshness-checked heartbeat cover their required dependencies and graceful shutdown paths.
- Completed locked CI gates, offline-model-request protection, representative local textbook
  fixture profiles and manifest template, an extraction validation checklist, and the initial
  retrieval question set. Copyrighted fixture PDFs and populated local manifests remain ignored.
- Ingestion execution and Docling extraction deliberately begin in Phase 1; the Phase 0 worker is
  the dependency-aware process host for that next slice.

### Work items

- Decide and document the supported Python runtime after verifying Docling, PyTorch, and pgvector client compatibility.
- Establish a `src`-based package structure with separate `tnpsc_book_rag` application and
  internal `tnpsc_rag` core packages.
- Define provider-neutral RAG contracts and protocols before implementing pgvector retrieval or
  PydanticAI generation adapters.
- Add typed settings loaded from environment variables.
- Define development, test, and provider-key environment templates without real secrets.
- Configure Ruff for formatting, linting, import hygiene, and selected security rules.
- Configure `ty` as the fast primary local type checker and CI gate.
- Configure Pyrefly in strict mode with warning-level diagnostics promoted to a failing independent
  CI type-checking gate.
- Pin both type checkers and upgrade them deliberately because their diagnostics are still evolving.
- Add guarded structlog JSON logging with stable event names, an explicit metadata allowlist, and
  request, trace, document, and ingestion-run correlation IDs.
- Establish OpenTelemetry tracing before business workflows are added.
- Default telemetry to metadata only; do not capture PDF text, prompts, evidence, or model output.
- Preserve correlation through async tasks and approved thread offloads; propagate W3C trace
  context and bounded correlation metadata explicitly across queued-worker boundaries.
- Suppress successful liveness, readiness, and metrics request noise while retaining probe failure
  events and request IDs.
- Define and integration-test middleware ordering, including generic pre-header 500 responses and
  post-header streaming failures.
- Create the Docker Compose topology for API, worker, PostgreSQL/pgvector, and shared artifacts.
- Add database migration tooling and the initial schema.
- Add API and worker health/readiness checks.
- Define the storage abstraction and safe artifact path conventions.
- Define domain enums for document state, ingestion stage, content type, and answer mode.
- Freeze the frontend-facing API v1 contract before endpoint implementation. Keep the normative
  behavior in `api_spec.md`, use checked-in `openapi.v1.yaml` for frontend generation, and verify
  FastAPI's generated implemented subset against both contracts as each route is delivered.
- Select two or three representative English textbook PDFs as development fixtures. Do not commit copyrighted or oversized fixtures unless redistribution is permitted.
- Create a small extraction validation checklist and an initial retrieval question set.

### Package boundaries

- `tnpsc_rag.models`: immutable query, filter, evidence, context, citation, and answer contracts.
- `tnpsc_rag.ports`: retrieval, context-assembly, and generation protocols.
- `tnpsc_rag.pipeline`: provider-neutral search and answer orchestration, implemented only after
  the corresponding phase exit gates are met.
- `tnpsc_book_rag.api`: HTTP routes and request/response schemas.
- `tnpsc_book_rag.catalog`: books, editions, and document lifecycle.
- `tnpsc_book_rag.ingestion`: job claiming and pipeline orchestration.
- `tnpsc_book_rag.extraction`: Docling adapter and normalized document representation.
- `tnpsc_book_rag.chunking`: deterministic chunk construction.
- `tnpsc_book_rag.adapters`: local embeddings, pgvector retrieval, and PydanticAI generation
  implementations of `tnpsc_rag` protocols.
- `tnpsc_book_rag.storage`: artifact storage implementation.
- `tnpsc_book_rag.db`: models, repositories, transactions, and migrations.
- `tnpsc_book_rag.observability`: logs, metrics, traces, and audit events.

Catalog and ingestion lifecycle values are defined in these application packages before the
database schema. Migrations and repositories must consume those exact values rather than defining a
second persistence-only state machine. `AnswerMode` remains in `tnpsc_rag` because it is part of the
provider-neutral answer contract.

Database delivery is split into reviewable revisions: `0001` establishes the pinned PostgreSQL/
pgvector service, async psycopg lifecycle, readiness check, and extension baseline; `0002` adds the
catalog, provenance, ingestion, and embedding tables; `0003` adds durable idempotent mutation
response snapshots. PostgreSQL integration tests always require an explicit disposable database
URL.

### Tests

- Settings validation tests.
- Migration up/down tests against a disposable database.
- Health-check tests.
- Storage path traversal and checksum tests.
- State-transition tests.
- Test-collection guard that blocks accidental real model requests.
- CI smoke checks for Ruff, `ty`, Pyrefly, and pytest.
- CI runs `uv lock --check` and invokes every quality gate with `uv run --locked` so a stale
  `uv.lock` fails instead of being repaired implicitly during a test job.

### Exit gate

- All containers start on the development laptop.
- pgvector is enabled and verified.
- API and worker report healthy.
- Migrations are repeatable on an empty database.
- Ruff, `ty`, and Pyrefly pass with a zero-error baseline.
- Logs include correlation fields and a sample request produces a trace.
- Successful probe traffic is absent from access events and traces, while probe failures remain
  observable.
- Async, thread, and queued-worker correlation tests pass without serializing free-form content.
- No extraction, embedding, or LLM dependency is required for a basic startup.

### Verification record (2026-07-16)

- `uv lock --check` passed.
- Ruff format/check, `ty`, and strict Pyrefly passed.
- Locked tests passed: 137 offline tests and 3 disposable-PostgreSQL tests.
- Docker images built successfully from the frozen lockfile.
- Compose migrations completed; API and worker containers reported healthy.
- `GET /health/live`, `GET /health/ready`, and `GET /v1/capabilities` returned the expected
  Phase 0 responses.

## 8. Phase 1 — Data Extraction and Image Preservation

**Status: in progress (2026-07-16).**

### Current implementation checkpoint

- Selected `backend/data/Std_06/term1/6th_Science_Term_I.pdf` as the first local fixture; PDFs under
  `backend/data/` remain ignored and are never committed.
- Added the Docling standard digital-PDF adapter with OCR and vision disabled, lossless embedded
  Docling JSON, page/reading-order provenance, image extraction, intrinsic dimensions, canonical
  thumbnails, captions, and explicit accessibility metadata.
- Added deterministic page-contained, structure-aware chunks and persisted pages, chunks, assets,
  and run fingerprints in one extraction transaction. No embeddings are generated in this phase.
- Added a queue-claiming worker path with context-preserving blocking work, heartbeat continuity,
  sanitized failures, and temporary-file cleanup.
- Verified the selected book directly through the adapter: 108 pages, 433 chunks, and 293 images.
  CUDA-first execution on the local RTX 3050 completed the same extraction in about 140 seconds;
  the earlier CPU run took about 8.5 minutes.
- Kept the production/runtime dependency set CPU-only. GPU extraction is intentionally isolated to
  the offline Colab/workstation package script so the API and worker do not carry CUDA libraries.
- Added `backend/scripts/extract_book.py`, an offline GPU-first package builder for Colab or a
  workstation. It produces lossless Docling JSON, page/chunk/asset JSONL, preserved images, runtime
  metadata, source provenance, and SHA-256 checksums, with atomic no-overwrite publication and an
  optional deterministic ZIP archive. The package was validated against the selected book with the
  same 108-page, 433-chunk, 293-image result.
- Added explicit curriculum identity to every extraction manifest: title, standard, subject, term,
  language, publisher, and edition. The twelve local Standard 6 packages were retrofitted with
  these fields and their payload checksums remain valid.
- Added a pure package-verification boundary that validates ZIP integrity, manifest checksums,
  curriculum metadata, page/chunk/asset counts, and provenance references before any database or
  artifact-storage writes.
- Added the application-side package materializer and importer. It verifies the offline archive,
  confirms its complete book identity plus source checksum and byte size against the catalog and
  stored PDF, retains the immutable ZIP under a run-scoped artifact key, stores Docling JSON plus
  content-addressed images/thumbnails, and persists package-v2 semantic parents and retrieval
  children in one transaction. Temporary materialized paths are never exposed outside the import
  context.
- Split the offline runtime into the dependency-light `tnpsc_extraction` package. The Colab script
  no longer imports the backend application package or its storage/configuration/observability
  side effects; an import-boundary test blocks OpenTelemetry and verifies the script still starts.
- Added metadata-only OpenTelemetry spans for the worker lifecycle and extraction stages
  (extraction, chunking, artifact storage, and persistence), with document/run correlation carried
  through blocking worker threads and exception messages excluded from span events.

For the current deployment shape, GPU extraction is performed offline with that script and the
production container remains CPU-oriented. The application importer now verifies the package
manifest, source SHA-256, file checksums, and extraction fingerprints before writing immutable
artifacts and derived records to PostgreSQL/object storage. Four Standard 6 Term I subjects now pass
the structural package gate, and the accepted Science v3/256 package passes the real application
import and PostgreSQL inspection gate. The frozen document, ingestion-history, page-detail,
printed-label correction, and chunk-inspection API reads are now implemented with filter-bound
keyset pagination and safe public projections. The worker now resolves queued source PDFs by
SHA-256 against a configured read-only package inbox, imports a unique verified package when one
is available, and otherwise retains the CPU extraction fallback. The remaining Phase 1 gates are
application CPU parity and targeted visual QA; they do not block the one-book retrieval demo.

### Planned migration: native Docling chunking and parent-child retrieval

**Status: implementation in progress.** The shared immutable parent/child values, reproducible
chunking configuration, native `TextbookChunker`, package-v2 offline writer, pure v2 verifier,
verified rechunk-only workflow, parent-child database schema, safe v1 backfill migration, and atomic
v2 repository persistence and importer are implemented. `textbook-hybrid-v3` additionally recovers
formula text from preserved Docling `orig`, retains unresolved formulas as page-linked
non-retrievable diagnostics, recognizes implicit definition prose, keeps Example-to-Solution
transitions together, filters corrupt derived text, and merges undersized children only within an
already-classified parent. The rechunk command upgrades the preserved v2 packages without another
PDF conversion. The package path now materializes and persists the native v2 graph. The v1
`chunk_pages()` path remains available and is persisted as one `mixed` parent per child until the
application CPU pipeline migrates. Do not re-extract the full corpus until the pilot configuration
and package-v2 contract have passed the quality gate below.

#### Representation policy

The migration will keep distinct representations because they serve different consumers:

1. `docling.json` is the canonical, lossless structured source for deterministic replay. Chunking
   must operate on the native `DoclingDocument`, never on whole-document Markdown or flattened page
   text.
2. Parent `display_text` is the faithful human/LLM evidence representation. Ordinary prose remains
   plain text. Complete tables use Markdown because it is inspectable, portable, and useful as an
   LLM-context fallback.
3. Table structure is also retained as structured JSON derived from Docling cells. Markdown is not
   the only copy of a table and is never treated as canonical.
4. Child `embedding_text` is produced by Docling contextualization using the exact embedding-model
   tokenizer. A table child may use a retrieval-oriented row/triplet representation even when its
   parent is displayed as Markdown.
5. Child `display_text` remains a faithful excerpt suitable for search results and citations.
   `display_format` records `plain_text` or `markdown`; consumers must never infer the format from
   punctuation.

This means Markdown remains necessary as a derived table display/citation representation, but the
pipeline does not convert the whole textbook to Markdown before chunking.

#### Fixed MVP decisions and deferred features

- Keep `BAAI/bge-small-en-v1.5`: 384-dimensional embeddings and a 512-token model limit.
- Pin both tokenizer identifier and immutable model revision. A floating Hugging Face revision is
  not allowed in a completed package or ingestion run.
- Use Docling `HybridChunker` with the pinned Hugging Face tokenizer and
  `repeat_table_header=true`.
- Begin the pilot with `merge_peers=false` so definitions, laws, activities, and worked examples
  cannot be merged merely because they share headings. Enable controlled merging only inside one
  already-classified parent if evaluation proves it useful.
- The four-book Standard 6 Term I structural pilot provisionally selects a 256-token maximum for
  embedding/retrieval evaluation. Its 95th-percentile child is roughly 189–209 tokens; the
  384-token variant removes only 48 children across the books and creates a substantially longer
  tail. Retain 384 as comparison evidence until labeled retrieval evaluation confirms 256. No
  child may exceed the selected limit.
- Begin with an 800-token parent soft target and a 1,200-token parent hard target. A complete table
  or solved example may exceed the soft target but must remain one logical parent.
- Keep OCR, formula enrichment, picture description, and vision-model calls disabled. Formula
  items whose decoded `text` is empty use Docling's preserved `orig`; a truly empty item remains
  visible in provenance but is ineligible for retrieval.
- Keep picture extraction enabled. Benchmark `images_scale=1.0` against `2.0` on selected dense
  diagrams before changing the default globally.
- Use the standard Docling PDF pipeline, which is threaded in the pinned Docling version. Configure
  `ThreadedPdfPipelineOptions` explicitly for reproducibility, but choose queue sizes, batch sizes,
  and timeouts from measurements on the development laptop. A 120-second document timeout is not
  acceptable because it is below already measured valid-book runtimes.
- Keep pipeline profiling disabled normally. Expose it only as an offline diagnostic flag and store
  bounded stage timings, never extracted content, in diagnostics.

#### Target content model

Add a semantic parent layer between pages and retrieval chunks:

`document -> content unit (parent) -> retrieval chunk (child) -> embedding`

Parent content-unit types initially are `section`, `prose`, `definition`, `law`, `solved_example`,
`activity`, `table`, `list`, `caption`, and `mixed`.

- A definition or law is one protected parent. If it fits the child token limit it produces exactly
  one child; an exceptional oversized definition produces multiple linked children without merging
  with adjacent material.
- A solved example parent contains the question, working, and solution. Children may split the
  parent for retrieval precision, but evidence expansion returns the complete solved example.
- A table parent contains the complete table, structured cell data, headings, caption, and Markdown
  display. Oversized tables produce row-group children with the column header repeated in every
  child.
- Ordinary prose is grouped within the inferred section hierarchy up to the parent target. It must
  not cross a protected semantic unit.
- Repeated headers, footers, `.indd` markers, and site-watermark noise remain in raw page/Docling
  data. Deterministic frequency plus bounding-box rules mark them ineligible for retrieval rather
  than deleting them.
- Every parent and child retains ordered Docling item references and complete page provenance.
  Cross-page parents use an ordered join rather than pretending they belong to one page.

#### Workstream 1 — Golden pilot and baseline

Before replacing the chunker:

- Select representative material from at least Science Term I and Mathematics Term I: a definition
  or law, a solved example, a short table, an oversized table, an activity box, a diagram caption,
  a multi-column page, and repeated footer noise.
- Record the current package IDs, page indexes, current chunks, and expected semantic boundaries.
- Add a small versioned evaluation manifest containing expected parent types, expected page spans,
  required table headers, and content that must be excluded from retrieval.
- Do not commit complete copyrighted books or extracted image collections as test fixtures. Store
  bounded fixture fragments or expected checksums/references and keep source PDFs locally ignored.
- Capture current chunk counts and token distributions so the migration can explain changes instead
  of merely producing different output.

#### Workstream 2 — One shared textbook chunker

Replace the regex-based `chunk_pages()` implementation with one shared `TextbookChunker` in the
dependency-light extraction runtime. The offline script and application must import this exact
implementation; neither path may contain a second copy of chunking rules.

The shared chunker will:

1. Load the lossless Docling JSON into a `DoclingDocument` or accept the identical in-memory
   document immediately after conversion.
2. Traverse native Docling items and construct deterministic semantic parent candidates with stable
   source references.
3. Classify protected definitions, laws, solved examples, activities, tables, and captions using
   explicit, tested English textbook rules. Classification must never call an LLM.
4. Run `HybridChunker` with the pinned BGE Small tokenizer for child segmentation and
   contextualization.
5. Prevent peer merging across parent boundaries. Any later prose merging is allowed only when the
   parent ID, headings, captions, and content type all match.
6. Produce a faithful child display representation and a separately contextualized embedding
   representation.
7. Calculate exact tokenizer counts from final `embedding_text` after headings, captions, and table
   headers have been added.
8. Emit deterministic parent/child sequence numbers, page spans, Docling references, and checksums.

The configuration fingerprint must include Docling version, chunker implementation version,
tokenizer identifier and revision, child/parent limits, merge policy, table serializer policy,
header repetition, noise-rule version, and normalization version.

#### Workstream 3 — Checksums and immutable domain values

Extend the shared extraction models with immutable `ExtractedContentUnit` and revised
`ExtractedChunk` values.

- Parent `content_sha256` covers the canonical parent display and structured content.
- Child `display_sha256` covers faithful child display text.
- Child `embedding_sha256` covers the exact final contextualized text sent to the embedding model.
- `chunk_embeddings.content_sha256` must correspond to `embedding_sha256`, not the display checksum.
- Token counts describe `embedding_text`, not raw text or Markdown alone.
- Parent and child IDs inside packages are deterministic package-local identifiers used to verify
  references before database UUIDs exist.

#### Workstream 4 — Extraction package manifest v2

Introduce an intentionally breaking package-v2 contract rather than silently changing v1
`chunks.jsonl` semantics. A v2 package contains:

- `manifest.json` with separate `extraction` and `chunking` sections.
- Lossless `docling.json`.
- `pages.jsonl` and `assets.jsonl`.
- `content_units.jsonl` containing semantic parents and optional structured table content.
- `chunks.jsonl` containing retrieval children and parent references.
- `images/` containing preserved picture assets.

The manifest `chunking` section records:

- Chunk schema and implementation versions.
- Tokenizer/model identifier plus immutable revision.
- Child maximum and parent soft/hard targets.
- Hybrid merge and repeated-table-header settings.
- Display and table serializer versions.
- Noise-classifier version.
- Chunking configuration fingerprint.

The v2 verifier must validate all existing file hashes and counts plus:

- Every child references exactly one known parent.
- Parent and child sequence numbers are contiguous and deterministic.
- All parent/child page and Docling references exist.
- Recorded token counts are positive and do not exceed the manifest child maximum.
- Display and embedding checksums match their payloads.
- Every table child belongs to a table parent; a split table child contains its header.
- No unlisted payload enters storage.

The verifier may continue recognizing v1 for diagnostics, but normal ingestion should reject v1
after the migration. The old packages remain local until all replacement v2 packages pass
verification.

#### Workstream 5 — Offline/local extraction script

Update `scripts/extract_book.py` to use the shared chunker after Docling conversion and before atomic
package publication.

- Add pinned tokenizer model/revision configuration with safe project defaults.
- Add child and parent token-limit options intended for controlled experiments; record resolved
  values in the manifest.
- Add an optional pipeline-profiling flag and bounded timing summary.
- Use explicit threaded PDF/table/layout settings, keeping OCR and enrichments disabled.
- Continue refusing overwrite and writing through a temporary sibling directory.
- Write `content_units.jsonl` and package-v2 child chunks.
- Print a bounded summary by parent type, child count, token percentiles, split-table count, excluded
  noise count, and warnings without printing textbook content.
- Update the Colab requirements explicitly for the Hugging Face tokenizer/chunking dependencies.
- Keep the import-boundary test proving that the script requires no FastAPI, database,
  OpenTelemetry, or application configuration imports.

The offline script remains the GPU extraction path. Tokenization/chunking does not require the GPU,
but running it in the same job guarantees that the published package is complete and verified.

#### Workstream 6 — PostgreSQL migration and persistence

**Complete and verified against PostgreSQL 18/pgvector.** Revision
`0005_parent_child_content` adds the parent tables, backfills each existing v1 chunk into a
one-to-one `mixed` parent, records exact child display and embedding checksums, and updates existing
embedding checksum references without regenerating vectors. The repository accepts native v2
graphs in one caller-owned transaction and retains a temporary one-parent-per-child adapter for the
legacy CPU worker. Fast schema, validation, and type tests plus real upgrade/downgrade/re-upgrade,
v1 backfill, native parent-child persistence, and transaction rollback tests pass against the
isolated Compose `tnpsc_test` database.

Add `content_units` and `content_unit_pages` tables. `content_units` stores document/run identity,
sequence, unit type, display text/format, optional structured JSON, section path, eligibility and
exclusion reason, checksums, and provenance. `content_unit_pages` stores ordered multi-page
provenance.

Revise `chunks` to:

- Require `content_unit_id` as the parent foreign key.
- Retain primary page plus ordered `chunk_pages` provenance for efficient citations.
- Store child display format, display checksum, embedding checksum, exact embedding token count,
  content type, contextualized embedding text, and Docling provenance.
- Index `content_unit_id`, document/run ordering, content type, and retrieval eligibility filters.

The migration must work both on an empty database and on a development database containing v1
chunks. Backfill each legacy chunk into a one-to-one `mixed` parent before making the parent foreign
key non-null; those backfilled records are for migration safety, not active v2 retrieval.

Update the persistence port and repository so pages, assets, parents, child chunks, and page joins
are written in one transaction. Persist the resolved chunker/tokenizer versions and fingerprint on
the ingestion run; do not hardcode `chunker_version = "1"` in the repository.

#### Workstream 7 — Application CPU pipeline and package importer

**Package importer, worker bridge, and inspection reads complete; application CPU parity remains.** The package-v2 path performs pure
verification, materializes typed parents and children, checks full catalog/source identity, stores
the immutable package and derived artifacts, and calls the same caller-owned parent-child
transaction used by the repository. A configured read-only inbox is indexed by verified source
checksum; the normal upload-created queue record is claimed by the worker and routed through the
package importer when a match exists, without adding a second public mutation contract. The
remaining work in this stream is migrating the optional CPU extraction worker to the shared
`TextbookChunker` and adding parity coverage from identical Docling JSON. Document,
ingestion-history, page, printed-label, and chunk inspection routes use a dedicated application
service and SQLAlchemy read adapter; they never expose storage keys, embedding text, or embedding
checksums.

On 2026-07-20, the verified 108-page Standard 6 Science Term I package completed an end-to-end
smoke import into the isolated PostgreSQL `tnpsc_test` database and temporary local artifact store:
108 pages, 548 semantic parents, 1,103 retrieval children, and 293 assets were persisted; the run
record retained `textbook-hybrid-v2` plus the pinned tokenizer revision; and structured table
parents remained Markdown with cell JSON. The temporary catalog graph was then deleted by cascade.

After the v3 pilot was accepted, the retained Science v3/256 package completed the same real
application import against a fresh dedicated disposable database: 108 pages, 578 semantic parents,
601 retrieval children, 293 assets, 605 parent-page links, and 626 child-page links were persisted.
All 29 table parents retained Markdown plus structured cell JSON, every asset retained intrinsic
dimensions and a generated thumbnail, the maximum child size was 253 tokens, and there were no
orphan parents or children above the configured 256-token limit. The one replacement-character
fragment remained explicitly non-retrievable. The run stored `textbook-hybrid-v3`, the pinned BGE
tokenizer revision, and the exact package configuration fingerprint. The catalog graph, temporary
artifact store, and dedicated database were removed after inspection.

The product-facing worker bridge was then smoke-tested through the running Docker API with the
original Standard 6 Social Science Term I PDF. Upload acceptance created the ordinary durable
queue record; the worker matched the source checksum to the mounted v3/256 package and completed
the verified import in about 22 seconds without invoking Docling. PostgreSQL contained the exact
package counts: 110 pages, 566 retrieval children, and 296 assets.

The normal application extraction path will become:

`source PDF -> Docling extraction -> lossless JSON -> shared parent/child chunker -> artifact storage -> one database transaction`

The package path will become:

`v2 ZIP -> pure verification -> temporary materialization -> source/catalog identity check -> immutable artifact storage -> same database transaction`

- Both paths must produce identical parent/child records when given the same lossless Docling JSON
  and chunking configuration.
- The package importer must validate title, standard, subject, language, publisher, and edition
  against the target catalog record in addition to the existing source checksum/size validation.
- Preserve the v2 ZIP under its run-scoped artifact key for audit and replay.
- Keep thumbnails and image storage content-addressed as today.
- Add metadata-only spans for Docling loading, semantic-parent construction, hybrid chunking,
  package verification, artifact storage, and persistence. Record counts, durations, versions, and
  error types only.
- A failure before the database transaction must not publish partial database content. Immutable
  orphan artifacts remain safe and are handled by a later explicit cleanup policy.

#### Workstream 8 — Retrieval and API implications

Phase 2 vector search operates on child chunks. It groups duplicate child hits by parent, retains
the best child score, and loads the parent plus a bounded sibling window for evidence assembly.
The complete parent is included when it is a definition, law, solved example, or table and fits the
evidence budget.

Keep child `chunk_id` as the citation/search identity so saved citations remain precise. Before
freezing the implementation, update the API specification to:

- Add `display_format` to chunk summaries and evidence records.
- Define table evidence as generated Markdown with no raw HTML, or add an optional structured-table
  projection if the frontend requires interactive cells.
- Make `GET /v1/sources/{chunk_id}` resolve and expose the matched child plus its expanded parent
  context without exposing internal embedding text.
- Keep `embedding_text`, embedding vectors, checksums, raw Docling metadata, and storage keys private.

Answer generation receives expanded parent evidence, but citations continue pointing to the exact
matched child and its page spans. Parent expansion must be deterministic and subject to the evidence
token budget.

#### Workstream 9 — Automated tests and parity checks

Add tests before corpus re-extraction:

- Unit tests for exact BGE tokenizer counts and contextualized-limit enforcement.
- Native Docling fixtures for headings, nested sections, definitions/laws, solved examples,
  activities, lists, captions, short tables, oversized tables, and cross-page units.
- Tests proving question and solution share a parent and that adjacent examples do not merge.
- Tests proving a short definition produces one child and an oversized definition splits only
  within its parent.
- Tests proving complete table structure and Markdown survive, all rows appear in child coverage,
  and split children repeat headers.
- Noise tests proving repeated headers/footers are excluded from retrieval but preserved in raw
  provenance.
- Determinism tests for sequence numbers and all checksums.
- Package-v2 corruption, path safety, count, checksum, and parent-reference tests.
- Database migration tests, transactional persistence tests, and idempotent artifact tests.
- A parity test showing the offline package path and application CPU path produce byte-equivalent
  parent/child JSON from the same lossless Docling document.
- Manual visual QA of the golden Science and Mathematics pages before accepting the configuration.

#### Pilot decision gate

Run only Science Term I and Mathematics Term I first. Compare 256- and 384-token child limits and,
where useful, Markdown versus retrieval-oriented table serialization. Freeze the configuration only
when all of these pass:

- No child exceeds the configured contextualized token maximum.
- Every retrieval child has exactly one parent and complete page provenance.
- Sample definitions/laws and solved examples match the expected protected boundaries.
- Every full table remains inspectable; split-table children repeat headers and cover every row.
- Repeated footer/header noise is absent from retrieval text but present in raw provenance.
- Local script and application pipeline parity is exact.
- Chunk boundaries and checksums are stable across two identical runs.
- A small labeled retrieval comparison shows no unacceptable loss against the current chunker.

On 2026-07-20, all eight 256/384 variants for English, Mathematics, Science, and Social Science
passed strict package-v2 publication verification under `textbook-hybrid-v3`. The migration reused
the exact source, Docling, page, asset, and image payloads. Formula placeholders fell to zero;
definitions became explicit protected parents; Example and Solution headings remained one semantic
parent; unsafe controls fell to zero; and irrecoverably corrupted fragments were marked
non-retrievable. One truly empty Mathematics formula is retained as an explicit page-72 diagnostic.
The remaining gates are application CPU parity, manual golden-page review, and labeled retrieval
evaluation after embeddings.

#### Corpus re-extraction and rollout order

After the pilot gate is approved:

1. Keep current v1 packages untouched as a rollback/reference set.
2. Rechunk the accepted v2 Docling payload into a new versioned output directory; re-extract the PDF
   only when the extraction payload itself changes. Never overwrite an existing package.
3. Verify each v2 package immediately after creation and retain its bounded summary.
4. Complete the three-book Phase 1 exit gate with structurally different subjects before running
   the entire Standard 6 corpus.
5. Import one verified package into a disposable PostgreSQL database and inspect its pages, parents,
   child chunks, tables, assets, and provenance.
6. Import the remaining verified packages only after the one-book database inspection passes.
7. Begin embeddings only after package verification, database persistence, and parent-child
   inspection are complete. Re-extraction and embedding must remain separate resumable stages.

Recommended implementation commits, each requiring approval, are: shared models/configuration;
native chunker plus fixtures; package-v2 local script; database migration/persistence; application
pipeline/importer; API/inspection updates; and pilot validation records. Dependency declarations
change through `pyproject.toml` or the extraction requirements file, and `uv` regenerates the lock;
the lockfile is never edited manually.

### Objective

Convert a digital textbook PDF into inspectable, page-linked text, chunks, and image assets without generating embeddings.

### Vertical slice

Start with one representative book and support the complete path:

`register book -> upload PDF -> queue ingestion -> extract -> inspect pages/chunks/assets`

Only after that slice works should ingestion be generalized to the remaining books.

### Work items

#### Upload and catalog

- Validate file type, size, and PDF readability.
- Compute source checksum before processing.
- Require standard, subject, title, language, and edition metadata.
- Persist the original PDF before creating an ingestion job.
- Reject or explicitly version duplicate checksums.

#### Docling extraction

- Configure the standard PDF pipeline without forced OCR.
- Detect whether a useful text layer exists.
- Preserve the lossless Docling JSON output.
- Capture page number, reading order, bounding boxes, item labels, and character spans where available.
- Record extraction warnings instead of silently dropping malformed elements.
- Fail fast with an unsupported-document reason for scan-only PDFs.

#### Page processing

- Build one normalized page record per PDF page.
- Preserve raw text exactly as extracted.
- Normalize line breaks, whitespace, Unicode, and repeated layout noise into a separate field.
- Identify recurring headers/footers carefully; preserve them in raw output even if excluded from retrieval text.
- Capture printed page labels where reliably available and allow later manual correction.

#### Image processing

- Export Docling picture assets in referenced-file mode.
- Store one canonical copy per asset checksum.
- Preserve page link, bounding box, caption, and Docling reference.
- Record intrinsic pixel dimensions for preserved raster assets so clients can reserve layout
  before loading their content.
- Generate one canonical aspect-preserving raster thumbnail during ingestion with the longest edge
  bounded by the deployment capability; record its dimensions and immutable artifact reference.
- Record explicit asset accessibility state: decorative, caption-derived alt text, manually
  remediated alt text, or unavailable. Do not send unavailable informative assets to the
  student-facing inline experience as if they were decorative.
- Do not generate page preview PNGs by default.
- Do not call a vision model.
- Define how an API will later authorize and serve asset files.

#### Chunking

- Chunk from the structured Docling document, not flattened PDF text alone.
- Prefer heading, paragraph, list, caption, and table boundaries.
- Keep chunks page-contained where possible.
- Preserve tables as coherent units or split them by meaningful row groups with repeated headers.
- Include book/chapter/section context in `embedding_text`, while keeping `display_text` faithful to the book.
- Target a conservative size below the embedding model's 512-token maximum; begin around 300–400 tokens and tune using real pages.
- Use tokenizer counts rather than character counts.
- Avoid arbitrary overlap; use small boundary overlap only when it improves continuity.
- Preserve sequence order for later neighbor expansion.

#### Worker behavior

- Claim jobs safely so only one worker processes a run.
- Update the stage after each durable milestone.
- Bound memory use and process one PDF at a time.
- Clean temporary files after success and retain useful diagnostics after failure.
- Make cancellation and retry behavior explicit.

### Inspection capability

Provide a development/admin way to inspect:

- Book and document metadata.
- Page text.
- Chunk boundaries and token counts.
- Extracted assets with their page and caption.
- Ingestion warnings and errors.

This can initially be an API or command-line inspection path; a frontend is not required.

### Tests

- Unit tests for normalization and deterministic chunking.
- Fixture-based extraction tests for paragraphs, headings, tables, and images.
- Checksum/idempotency tests.
- Failure tests for encrypted, corrupted, empty, and scan-only PDFs.
- Integration test covering upload through completed extraction.
- Manual visual comparison of selected PDF pages against stored page/chunk/asset provenance.

### Quality checks

- Every extracted page maps to the correct PDF page.
- Every chunk resolves to at least one page.
- No chunk exceeds the chosen tokenizer limit.
- Important headings and table headers are retained.
- Extracted images open successfully and map to the correct page.
- Re-ingestion with the same configuration produces stable chunk checksums.

### Exit gate

- At least three structurally different English textbooks ingest successfully.
- Page and asset provenance is manually verified on a representative sample.
- Scan-only and malformed files fail clearly.
- The extraction output is useful to inspect without an LLM.
- No major content class is silently lost.

## 9. Phase 2 — Local Embeddings and Semantic Retrieval

### Objective

Deliver a useful, completely local semantic-search endpoint backed by pgvector.

### Initial embedding decision

- Start with `BAAI/bge-small-en-v1.5`.
- Store 384-dimensional normalized vectors.
- Pin the model revision rather than relying on a floating remote revision.
- Cache the model in a controlled location.
- Prefer CPU inference initially for a simpler Docker setup; benchmark optional CUDA use separately.
- Use the same tokenizer and model configuration for document and query embeddings.

### Work items

#### Embedding pipeline

- Batch chunk embedding in the ingestion worker.
- Skip unchanged chunks using the content checksum and model revision.
- Persist model metadata with each embedding batch.
- Mark the document ready only after all active chunks have valid embeddings.
- Provide a controlled re-embedding operation for a changed model or preprocessing rule.

#### Retrieval query

- Implement the pgvector-backed retriever as an application adapter satisfying the
  `tnpsc_rag` retrieval protocol.
- Generate a normalized query embedding locally.
- Use cosine similarity consistently.
- Begin with exact pgvector search.
- Apply book, standard, subject, edition, and language filters in SQL.
- Return stable ordering for tied scores.
- Do not add HNSW until corpus size and measured latency justify it.

#### Search API

`POST /v1/search` should support:

- Query text.
- `top_k` with safe bounds.
- Optional standard filter.
- Optional subject filter.
- Optional book/document filters.
- Optional inclusion of neighboring chunk metadata, disabled by default.

Each result should include:

- Chunk ID and display text.
- Retrieval score with clear model-specific semantics.
- Book title, edition, standard, and subject.
- Printed page label and PDF page index.
- Chapter/section path.
- Associated captioned assets or asset references when relevant.
- No generated explanation.

#### Indexing

- Add ordinary indexes for document state and common metadata filters.
- Measure exact-search performance first.
- If required later, add HNSW with a recorded recall comparison against exact search.

### Retrieval evaluation set

Create a versioned benchmark containing:

- Direct factual questions.
- Questions that paraphrase the textbook wording.
- Chapter/section lookup queries.
- Questions whose answer is in a table or caption.
- Similar concepts appearing in multiple grades or books.
- Unanswerable/out-of-corpus questions.

For each answerable query, record one or more relevant chunk/page IDs rather than only a natural-language expected answer.

### Metrics

- Recall@1, Recall@5, and Recall@10.
- Mean reciprocal rank.
- Filter correctness.
- Page/citation correctness.
- Query-embedding latency.
- Database-search latency and total endpoint latency.
- Empty/irrelevant result behavior.

Initial targets should be agreed after the first benchmark is labeled. A practical starting gate is Recall@5 of at least 0.85 on the curated MVP set and p95 search latency under one second on the development laptop at expected corpus size.

### Tests

- Deterministic query embedding tests within numerical tolerance.
- Retrieval tests with known nearest neighbors.
- Metadata-filter isolation tests.
- Empty query and invalid `top_k` validation.
- Corpus activation tests ensuring incomplete documents never appear.
- API contract tests.
- Offline test proving search works without provider API keys or network access.

### Exit gate

- Semantic search returns relevant, cited chunks for the benchmark.
- Retrieval metrics meet the agreed threshold.
- Search works when all LLM provider keys are absent.
- Latency is acceptable with exact search.
- Known retrieval failures are documented before generation begins.

## 10. Phase 3 — Context Augmentation and Citation Assembly

### Objective

Transform raw search results into a compact, deterministic evidence package suitable for an LLM, without calling an LLM yet.

### Work items

- Finalize the initial `EvidencePack` contract in `tnpsc_rag` without adding provider behavior.
- Retrieve a larger candidate set than is ultimately sent to generation.
- Remove duplicate or highly overlapping chunks.
- Preserve ranking while limiting repeated evidence from the same page.
- Add preceding/following chunks only when needed for continuity.
- Distinguish semantic hits from neighbor-expanded context.
- Enforce a configurable context-token budget.
- Preserve chapter and section headings.
- Serialize tables without losing header meaning.
- Associate relevant captioned assets as metadata; do not send image pixels to a text-only model.
- Assign stable citation labels to evidence records.
- Record why each context item was included: semantic match, neighbor, or structural parent.
- Define behavior when no evidence meets the retrieval policy.

### Answer-mode policies

#### `textbook_only`

- Context contains only retrieved textbook evidence and its metadata.
- The future generator must be allowed to abstain.
- No system-supplied general facts are added.

#### `textbook_plus_general`

- The same textbook evidence is supplied first.
- The future generator is explicitly allowed to add general explanation.
- The output contract must distinguish sourced textbook claims from unsourced supplementary material.

### Inspection and debugging

Add an internal or development-only context preview that shows:

- Original query.
- Search parameters.
- Ranked hits.
- Neighbor expansions.
- Final token count.
- Citation mapping.
- Exclusion reasons.

### Tests

- Deterministic evidence-pack snapshots.
- Token-budget enforcement.
- Deduplication and neighbor-expansion tests.
- Citation-label stability.
- Table and caption preservation tests.
- No-evidence behavior.
- Tests ensuring inactive or filtered documents cannot enter the context indirectly.

### Exit gate

- A developer can inspect exactly what an LLM would receive.
- Context fits the configured budget.
- Every evidence item resolves to valid book/page provenance.
- Both answer modes have explicit, testable policies.
- No provider integration is required to test augmentation.

## 11. Phase 4 — PydanticAI v2 Answer Generation

### Objective

Generate explanations from the proven retrieval and augmentation pipeline while keeping provider behavior replaceable and auditable.

### Provider approach

- Implement PydanticAI as an application adapter satisfying the `tnpsc_rag` generation protocol.
- Keep prompts, evidence policies, and citation validation provider-neutral in `tnpsc_rag`.
- Evolve the preliminary `GenerationResult` into closed text/citation segment contracts before
  implementing the provider adapter; do not recover citation positions by parsing model prose.
- Use the stable PydanticAI v2 integration inside the API application boundary.
- Use typed Pydantic output models for answers, citations, abstention, and supplementary sections.
- Keep the agent minimal: no tools, graph, MCP, multi-agent workflow, or durable execution is required.
- Configure native Groq, OpenRouter, and Mistral providers through environment/settings.
- Use PydanticAI's `FallbackModel` as the only fallback layer.
- Prefer Groq free access as the initial primary when available.
- Use OpenRouter `:free` models as a fallback, not as a guaranteed dependency.
- Support a Mistral provider or Mistral-family model when free quota is available.
- Do not assume any named free model will remain available.
- Pin the PydanticAI major version and provider SDK versions.
- Consider a separate LiteLLM proxy later only if multiple applications need shared keys, quotas, routing, or accounting.

### Answer API

`POST /v1/answers` should accept:

- Query text.
- `mode`: `textbook_only` or `textbook_plus_general`.
- The same metadata filters as semantic search.
- Bounded retrieval and response-length controls.
- `application/json` and opt-in `text/event-stream` response representations for the same request.

The answer response should include:

- Generated explanation as ordered text/citation blocks rather than client-parsed citation syntax.
- Answer mode actually applied.
- Structured citations.
- Exact evidence chunks used.
- An abstention or insufficient-evidence indicator.
- Supplementary-material marker when applicable.
- Request ID for diagnostics.

Persist the public answer-run state needed for `GET /v1/answers/{answer_id}` so an interrupted SSE
client can recover without replaying generation. Retain running/final state, the validated
structured result or sanitized problem, timestamps, and request ID for at least the capability
endpoint's advertised retention window.

The SSE representation emits progress and explicitly provisional text deltas, may reset that
preview when fallback or validation restarts generation, and terminates with the same validated
structured answer as the JSON representation. Only the terminal answer is canonical.

Provider/model details may remain internal but must be recorded in `answer_runs`.

### Strict grounding behavior

- In `textbook_only`, every material factual claim should have a citation.
- Citation IDs must be validated against the supplied evidence pack.
- Unknown citation IDs cause validation failure or regeneration, never silent acceptance.
- The model must state that the evidence is insufficient when it cannot answer from the context.
- Prompt instructions alone are not considered enforcement.
- Add a lightweight post-generation validator for citation existence, source membership, and response structure.

### Supplemented behavior

- Textbook-supported explanation remains primary.
- General model knowledge is clearly labeled as supplementary.
- General material must not receive textbook citations.
- The response must not imply that supplementary claims occur in the cited page.

### Reliability

- Use bounded timeouts.
- Retry only transient failures.
- Honor `Retry-After` when available.
- Fall back on rate-limit, timeout, and service-unavailable errors.
- Cap total attempts and total request duration.
- Return a clear capacity error when all free providers are unavailable.
- Never degrade or change the requested answer mode during fallback.
- If fallback or validation retry occurs after provisional SSE text has been emitted, send an
  explicit reset before streaming replacement preview text.
- Keep `/v1/search` healthy during all generation-provider failures.

### Prompt and model versioning

- Version system prompts and response contracts.
- Record the prompt version, provider, model, and evidence IDs for each answer.
- Evaluate a model before adding it to the fallback pool.
- Do not rely on provider-specific structured-output features unless the selected fallback models all support them; retain a validated portable response path.

### PydanticAI testing and observability

- Set `ALLOW_MODEL_REQUESTS=False` in the default test environment.
- Use `TestModel` for schema and agent-flow tests.
- Use `FunctionModel` for deterministic domain-specific model behavior and failure cases.
- Use dependency/model overrides rather than patching network clients deep inside application code.
- Instrument agent runs with OpenTelemetry.
- Use telemetry format version 5 or the current non-deprecated default.
- Set `include_content=False` by default so prompts, evidence, completions, and binary assets are not exported.
- Record provider/model, token counts, duration, retries, and fallback decisions without recording secrets.
- Introduce Pydantic Evals for versioned answer-quality datasets after deterministic tests are stable.

### Privacy and security

- Free hosted generation sends the query and selected textbook excerpts to a third party.
- Document this behavior clearly.
- Never send the complete textbook when a small evidence pack is sufficient.
- Redact keys from errors and logs.
- Restrict provider keys to the backend.

### Tests

- PydanticAI `TestModel` and `FunctionModel` tests for both answer modes.
- Structured answer-block tests proving every citation node resolves to returned evidence without
  frontend string parsing.
- SSE tests for event order, provisional reset, keepalive behavior, disconnect cancellation, and
  exactly one terminal completion or failure event.
- Citation validation tests, including fabricated IDs.
- Abstention tests for missing evidence.
- Supplement-labeling tests.
- Timeout, 429, provider outage, and fallback tests.
- Test proving provider failure does not break semantic search.
- Opt-in live smoke tests that do not run in the normal test suite.

### Evaluation

- Answer correctness against textbook evidence.
- Citation precision and citation completeness.
- Groundedness/faithfulness review.
- Abstention accuracy.
- Leakage of general knowledge into `textbook_only`.
- Clear labeling of supplementation.
- Latency and provider failure rate.

### Exit gate

- Both modes meet their documented behavior on the benchmark.
- Fabricated citations are rejected.
- Strict mode abstains when evidence is insufficient.
- Supplemented claims are visually and structurally distinct.
- Provider exhaustion returns a controlled error while search remains operational.
- Normal CI cannot make a real model request.

## 12. Phase 5 — MVP Hardening and Release Readiness

### Objective

Make the validated pipeline repeatable, supportable, and safe for an initial user-facing MVP.

### Work items

#### Operational hardening

- Add graceful startup/shutdown and worker lease recovery.
- Add database backup and artifact-backup instructions.
- Perform a restore drill; a backup is not considered valid until it has been restored successfully.
- Add migration and rollback procedures.
- Add re-ingestion and re-embedding administration paths.
- Add disk-space monitoring and artifact-retention rules.
- Set resource limits appropriate for the laptop.
- Confirm one-document ingestion does not starve API queries.
- Add bounded database connection pools and verify pool exhaustion behavior.
- Add worker dead-letter/manual-recovery behavior for repeatedly failing jobs.

#### Observability

- Emit structured JSON logs to stdout with service, environment, request ID, trace ID, document ID, ingestion-run ID, stage, duration, status, and stable error code when applicable.
- Sanitize log values to prevent log injection and never log keys, vectors, complete textbook content, prompts, evidence packs, or model responses by default.
- Trace HTTP requests, worker jobs, extraction stages, embedding batches, database retrieval, augmentation, provider calls, and citation validation with OpenTelemetry.
- Propagate trace context across API, database operations, and worker jobs where practical.
- Export locally to a lightweight OpenTelemetry viewer or console; do not require a heavy dashboard stack for development.
- Measure ingestion duration, page/chunk/asset counts, extraction failures, queue depth/age, search latency, zero-result searches, embedding duration, answer latency, provider rate limits/fallbacks, citation failures, abstentions, and disk space.
- Keep metrics low-cardinality: do not use query text, request IDs, book IDs, document IDs, or user IDs as labels.
- Correlation from answer run to retrieval evidence and document version.
- Health/readiness checks that distinguish database, storage, embedding, and optional provider health.
- Define alert conditions only after a production SLO exists; local development relies on logs and traces.

#### API hardening

- Consistent error schema.
- Request-size and timeout limits.
- Safe file handling and path validation.
- File magic/type validation rather than trusting the filename or client MIME type.
- Limits for PDF size, page count, extraction time, query length, `top_k`, context tokens, and generated output.
- Rate limiting and concurrency limiting at the API boundary before public exposure.
- Authentication and authorization for book upload, reprocessing, deletion, and other administrative operations.
- Idempotency keys or checksum-based idempotency for upload and ingestion requests.
- Pagination for catalog endpoints.
- OpenAPI examples for search, answer modes, citations, and errors.
- A public capabilities endpoint for runtime feature and limit discovery.
- An exact browser contract for allowed methods/headers, exposed response headers, explicit
  origins, credentials policy, and preflight caching; test direct-browser and same-origin proxy
  deployments.
- Verify reverse proxies do not buffer SSE answer responses and that disconnect cancellation is
  bounded and observable.
- CORS configuration conforming to the frozen SvelteKit browser contract.

#### Quality and CI

- Ruff formatting and linting.
- Blocking `ty` and Pyrefly checks with pinned versions.
- Unit, property-based, integration, end-to-end, and evaluation test layers.
- Tests run with real PostgreSQL/pgvector rather than SQLite substitution.
- Default test runs block network model access.
- Migration test in CI.
- Retrieval regression benchmark with stored results.
- PydanticAI deterministic generation regression suite.
- Opt-in live provider smoke tests outside normal CI.
- Reproducible container builds with locked dependencies and pinned base-image versions.
- Dependency, container, secret, and license scanning before a production release.
- Generate an SBOM for release images when deployment begins.

#### Documentation

- Local setup and teardown.
- Required and optional environment variables.
- How to ingest a textbook.
- How to inspect failed ingestion.
- Search and answer API usage.
- How to add or replace a PydanticAI provider/model and update the fallback order.
- Logging, tracing, metrics, privacy, and retention behavior.
- Backup and tested restore procedures.
- Operational runbooks for stuck ingestion, provider exhaustion, database failure, and low disk space.
- Known MVP limitations.

### Exit gate

- A clean machine can start the stack from documented steps.
- A representative book can be ingested and queried end to end.
- Retrieval benchmark results are recorded and reproducible.
- Both answer modes are demonstrable with a configured free provider.
- Provider unavailability, failed ingestion, and low disk space produce actionable errors.
- The MVP limitations are explicit.

## 13. API Delivery Order

The resource layout and public schemas are frozen in `api_spec.md` and `openapi.v1.yaml`. Deliver
the routes in this order, updating both implementation-status sources as each slice lands:

1. `GET /health/live` and `GET /health/ready`.
2. `GET /v1/capabilities`, CORS conformance, and catalog listing/detail operations.
3. `POST /v1/books` and `POST /v1/books/{book_id}/documents` for catalog mutation and PDF upload.
4. Global/document ingestion history, detail, and failed-run retry operations.
5. Development/admin page and chunk inspection plus asset metadata/content/thumbnail endpoints.
6. `GET /v1/sources/{chunk_id}` and `POST /v1/search`.
7. Internal development-only context preview, outside the public v1 contract.
8. `POST /v1/answers` with both JSON and SSE representations, then durable answer recovery.
9. Contract-compatible refinements needed by the frontend.

Search and answer remain separate operations. Breaking public contract changes require a new API
version; additive response fields follow the compatibility rules in `api_spec.md`.

## 14. Testing Strategy by Layer

### Static quality gates

- Ruff checks formatting, imports, common defects, and selected security rules.
- `ty` is the primary fast local type checker and a blocking CI check.
- Pyrefly runs with the strict preset as an independent blocking CI check.
- Both type checkers cover `src` and tests from the beginning of the project.
- Type-checker versions are pinned; upgrades happen in dedicated changes so new diagnostics are reviewable.
- Suppressions are narrow, rule-specific, and include a reason. A global baseline of ignored errors is not accepted for this new codebase.

### Unit tests

- Use pytest with AnyIO support for asynchronous application code.
- Use pytest-cov/coverage.py with branch coverage to reveal untested paths; do not optimize for a vanity 100% target.
- Text normalization.
- Chunking and token limits.
- State transitions.
- Storage paths and checksums.
- Evidence selection and citation mapping.
- Prompt/output validation.
- Provider error classification and fallback decisions.
- Pure business logic runs without Docker, PostgreSQL, Docling, or network access where feasible.

### Property-based tests

- Use Hypothesis for invariant-driven tests.
- Whitespace and Unicode normalization invariants.
- Chunk size, order, and provenance invariants.
- State-machine transitions and idempotent retries.
- Bounded request values such as `top_k` and context limits.
- Citation IDs can never escape the supplied evidence set.

### Integration tests

- PostgreSQL migrations and repositories against real PostgreSQL with pgvector.
- Docling extraction of small synthetic or redistributable fixtures containing headings, tables, and images.
- pgvector retrieval and filters.
- Worker job claiming and retry.
- Artifact persistence.
- FastAPI request/response contracts through an in-process HTTP client.
- PydanticAI integration through `TestModel` and `FunctionModel`.

### End-to-end tests

- PDF upload through extracted document.
- PDF upload through searchable document.
- Search with page-correct citations.
- Strict answer with valid evidence.
- Strict abstention without evidence.
- Supplemented answer with clear labeling.
- Provider outage while local search remains available.
- Worker interruption, lease expiry, retry, and recovery.
- Duplicate upload and safe re-ingestion.

The primary end-to-end test is deterministic and offline:

`PDF upload -> extraction -> chunking -> embedding -> search -> evidence pack -> FunctionModel answer -> citation validation`

### Retrieval evaluation

- Maintain a versioned dataset mapping each query to relevant chunk/page IDs.
- Report Recall@1, Recall@5, Recall@10, mean reciprocal rank, filter correctness, and latency.
- Compare approximate indexes against exact search before enabling them.
- Fail the retrieval regression gate when an accepted baseline degrades beyond an agreed tolerance.

### Generation evaluation

- Keep evaluation datasets separate from ordinary unit-test fixtures.
- Evaluate correctness, citation precision/completeness, groundedness, abstention, supplementary-label clarity, and latency.
- Use deterministic evaluators where possible; LLM-as-judge evaluation is supplementary and never the only release gate.
- Version the prompt, model/fallback configuration, evidence policy, dataset, and evaluator together in reports.
- Evaluate a provider/model before adding it to the production fallback chain.

### Network policy

- Normal unit, integration, and end-to-end tests do not call hosted models.
- PydanticAI model requests are globally disabled unless a test explicitly opts in.
- Live provider smoke tests use a separate marker, explicit environment opt-in, and strict request/token caps.
- Provider quota exhaustion never makes the normal CI suite flaky.

### Manual QA

- Visual comparison of PDF pages and extracted content.
- Inspection of tables and image captions.
- TNPSC-style question review.
- Citation navigation from answer to correct page and asset.
- Restore-drill verification and operational runbook rehearsal before production.

## 15. Risk Register

| Risk | Effect | Mitigation |
|---|---|---|
| PDF reading order is incorrect | Poor chunks and retrieval | Inspect Docling structure, retain raw JSON, add fixture regressions |
| Printed page differs from PDF page | Misleading citations | Store both fields and allow correction |
| Important content exists only in images | Text RAG cannot answer it | Preserve assets now; defer multimodal retrieval explicitly |
| Chunking splits definitions or tables | Reduced retrieval quality | Structure-aware chunking and retrieval benchmark cases |
| Embedding scores appear high for unrelated text | Bad threshold assumptions | Evaluate relative rank; calibrate any threshold on the corpus |
| Free LLM model disappears | Answer endpoint outage | Configured PydanticAI `FallbackModel` chain and controlled capacity error |
| Free quota is exhausted | Generation unavailable | Keep search local; cache only safe identical outputs; expose retry information |
| Provider changes output format | Citation parsing failures | Portable response contract and backend validation |
| LLM fabricates citations | False trust | Server-side citation membership validation and abstention policy |
| Disk usage grows unexpectedly | Failed local development | No page renders by default, checksum deduplication, cleanup and monitoring |
| Python/ML dependency mismatch | Build failure | Verify runtime in Phase 0 and pin reproducible dependency versions |
| Re-ingestion changes chunk IDs | Broken audit trails | Stable checksums, versioned runs, immutable historical document versions |
| Malicious or pathological PDF consumes resources | Worker outage or denial of service | Validate uploads and isolate parsing with CPU, memory, page-count, file-size, and timeout limits |
| Public admin endpoint is abused | Corpus corruption or deletion | Require authentication and explicit authorization for all mutation/admin operations |
| Telemetry leaks prompts or textbook content | Privacy or licensing exposure | Metadata-only telemetry by default and `include_content=False` for PydanticAI |
| Backup exists but cannot be restored | Irrecoverable data loss | Scheduled restore drills and documented RPO/RTO |
| Two type checkers disagree | Developer friction | Pin versions, use narrow documented suppressions, and validate behavior with runtime tests |
| Model/prompt change silently lowers quality | Retrieval or answer regression | Versioned eval gate before rollout and rollback to the previous configuration |

## 16. Production Service Checklist

The local MVP is not automatically production-ready. The following controls become mandatory before the service is exposed to untrusted users or relied upon as a persistent shared service.

### 16.1 Hosting and network boundary

- Treat the laptop Docker Compose setup as development infrastructure, not a production topology.
- Place the public API behind TLS termination with correctly configured trusted-proxy and forwarded-header handling.
- Keep PostgreSQL, artifact storage, worker controls, metrics, traces, and debug endpoints off the public network.
- Restrict administrative routes by both authentication and network policy where practical.
- Use durable production storage or managed PostgreSQL/object storage with explicit persistence and backup guarantees.
- Choose redundancy and high availability from the agreed SLO/RPO/RTO rather than adding distributed infrastructure by default.
- Document every accepted single point of failure in the first production deployment.

### 16.2 Identity and authorization

- Decide whether search is public, authenticated, or both.
- Require authentication for uploads, metadata edits, activation, deletion, reprocessing, re-embedding, and operational endpoints.
- Separate reader and administrator permissions even if the first production deployment has only one administrator.
- Enforce authorization in the backend; hiding controls in the frontend is not authorization.
- Use short-lived or rotatable credentials and document emergency revocation.
- Keep health endpoints minimal and protect debug, metrics, trace, and context-preview endpoints.

### 16.3 Abuse prevention and resource governance

- Apply per-client rate limits and global concurrency limits.
- Bound request body size, PDF size, page count, query length, `top_k`, filter breadth, context tokens, response tokens, processing time, and retry count.
- Add backpressure so uploads cannot create an unbounded worker queue.
- Reserve capacity for health checks and ordinary search while ingestion is busy.
- Return standard `413`, `429`, timeout, and capacity errors with safe retry guidance.
- Load-test search, ingestion, and mixed workloads at expected and failure-level concurrency.
- Define a degradation policy: search stays available when generation providers fail; uploads may pause before search is impacted.

### 16.4 Secure document processing

- Treat every uploaded PDF as untrusted input.
- Validate the file signature and parser result rather than trusting the filename or MIME header.
- Generate storage names server-side and store uploads outside a directly executable/web-served path.
- Reject encrypted, corrupted, unexpectedly huge, scan-only, or unsupported documents with stable error codes.
- Run Docling in an unprivileged worker/container with constrained CPU, memory, temporary storage, and wall-clock time.
- Prevent uploaded documents from selecting local paths, arbitrary output paths, or unexpected remote URLs.
- Patch PDF, image, ML, and archive-processing dependencies promptly.
- Consider malware scanning or content disarm/reconstruction if document upload is opened beyond trusted administrators.

### 16.5 Secrets and supply-chain security

- Store provider and database credentials in the deployment secret mechanism, not images, Compose files, source control, or logs.
- Support key rotation without rebuilding application images.
- Lock Python dependencies and pin container base images intentionally.
- Run dependency, container, secret, and license scans in CI.
- Generate an SBOM for production images.
- Run containers as non-root, drop unnecessary Linux capabilities, and prefer read-only filesystems with explicit writable mounts.
- Keep development/debug settings disabled in production.

### 16.6 Data integrity, privacy, and legal review

- Confirm permission to ingest, retain, excerpt, and display textbook pages and images.
- Document which query/evidence content is sent to each hosted LLM provider and review provider retention/training terms.
- Default traces and logs to content-free metadata.
- Define retention and deletion rules for PDFs, extracted assets, failed-ingestion artifacts, queries, answers, audit records, backups, and telemetry.
- Make deletion cover database records, artifact storage, caches, and future backups according to the documented retention policy.
- Encrypt traffic in transit and production data/backups at rest.
- Record document source, checksum, edition, ingestion configuration, and activation history for auditability.

### 16.7 Availability and graceful degradation

- Define service-level objectives separately for local search, ingestion, and hosted answer generation.
- Treat free hosted generation as best-effort unless a provider SLA is purchased later.
- Use bounded timeouts, circuit breaking, fallback limits, and load shedding.
- Make worker leases recoverable after process or host failure.
- Add a dead-letter/manual-remediation path for poison documents.
- Test API restart, worker restart, database restart, provider outage, storage-full, and partial-ingestion scenarios.
- Ensure shutdown drains or safely abandons in-flight work without corrupting activation state.

### 16.8 Backup and disaster recovery

- Back up PostgreSQL and artifact storage as one documented recovery set.
- Define recovery point objective and recovery time objective.
- Encrypt backups and keep at least one copy outside the primary host/storage failure domain.
- Automate backup success monitoring.
- Perform scheduled restore drills and verify checksums, migrations, active document versions, assets, and search behavior after recovery.
- Add PostgreSQL WAL archiving and point-in-time recovery when the required RPO justifies it.
- Do not classify a backup strategy as complete until restore time has been measured.

### 16.9 Deployment and change management

- Separate development, test/staging, and production settings and credentials.
- Build one immutable image and promote it rather than rebuilding differently per environment.
- Use backward-compatible expand/migrate/contract database changes where rolling deployment is required.
- Define application and migration rollback procedures.
- Gate dependency, embedding-model, chunker, prompt, provider, and fallback changes with their relevant regression suites.
- Store the last-known-good model/prompt/retrieval configuration for rapid rollback.
- Use a canary or restricted rollout for changes that can affect answer quality.

### 16.10 Observability, SLOs, and incident response

- Define service indicators for availability, search latency, ingestion success/time, queue age, generation success/latency, citation validation, and disk/database capacity.
- Create alerts from SLOs and capacity thresholds rather than every individual exception.
- Maintain dashboards or saved queries that link symptoms to trace and run IDs.
- Set log, trace, and metric retention and sampling policies.
- Write runbooks for provider exhaustion, slow/failed ingestion, stuck jobs, low disk, high database connections, backup failure, and bad deployment.
- Assign ownership for responding to alerts before calling the service production.
- Review telemetry cardinality and content exposure before each new instrumented feature.

### 16.11 API and client compatibility

- Publish an explicit API versioning and deprecation policy.
- Preserve stable error codes even when human-readable messages change.
- Add idempotency behavior for mutation requests.
- Define pagination cursors/order and maximum page sizes.
- Support opt-in exact collection counts without making count queries the infinite-scroll default.
- Use ETags or equivalent cache validators for immutable assets where useful.
- Document timeout, retry, and rate-limit behavior for clients.
- Treat `openapi.v1.yaml` as the machine contract for client generation and fail CI when its
  implemented subset drifts from FastAPI's generated schema or normative examples.
- Test every documented JSON example and known SSE payload against the checked-in schemas.
- Never expose internal filesystem paths, provider errors, SQL details, or stack traces.

### 16.12 Production security and quality gates

- Perform an API threat model covering authorization, resource exhaustion, unsafe file processing, secrets, provider dependencies, and data exposure.
- Review the service against the OWASP API Security Top 10 before public exposure.
- Run automated vulnerability scans and remediate release-blocking findings.
- Test authorization failures, rate limiting, oversized uploads, parser timeouts, malformed PDFs, log sanitization, and secret redaction.
- Require successful static checks, deterministic tests, database tests, extraction fixtures, retrieval evaluation, generation evaluation, container scans, and backup restore evidence for a production release.

### 16.13 Production exit gate

- Administrative mutations require tested authentication and authorization.
- Untrusted uploads are isolated and resource-bounded.
- Search and mixed-workload load tests meet the agreed SLOs.
- Database and artifact backups have passed a restore drill.
- Alerts and runbooks cover the critical failure modes.
- Content retention, provider data handling, and textbook rights are documented.
- Release and rollback paths are rehearsed.
- A provider outage degrades only answer generation, not local search or corpus integrity.

Reference baselines:

- [OWASP API Security Top 10](https://owasp.org/API-Security/editions/2023/en/0x10-api-security-risks/)
- [OWASP File Upload Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/File_Upload_Cheat_Sheet.html)
- [OWASP REST Security Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/REST_Security_Cheat_Sheet.html)
- [PostgreSQL backup and point-in-time recovery](https://www.postgresql.org/docs/current/continuous-archiving.html)
- [PydanticAI testing](https://pydantic.dev/docs/ai/guides/testing/)
- [PydanticAI OpenTelemetry instrumentation](https://pydantic.dev/docs/ai/integrations/logfire/)

## 17. MVP Definition of Done

The backend MVP is complete when all of the following are true:

- At least three representative English digital textbooks ingest successfully.
- Original PDFs, lossless extraction output, and document images are preserved.
- Every searchable chunk has correct book, edition, and page provenance.
- Search works locally without an LLM provider.
- Retrieval meets the agreed benchmark threshold.
- `/v1/search` returns ranked chunks with structured citations and no generated prose.
- `/v1/answers` supports both answer modes.
- `textbook_only` answers cite retrieved evidence or abstain.
- `textbook_plus_general` clearly distinguishes supplementary knowledge.
- Invalid citations are rejected by the backend.
- Free-provider exhaustion does not affect extraction or search.
- Ruff, `ty`, Pyrefly, deterministic tests, and retrieval evaluation pass.
- PydanticAI tests cannot make accidental live provider requests.
- Logs and traces correlate API, ingestion, retrieval, and generation work without exporting content by default.
- The Docker development environment, migrations, ingestion, testing, and provider configuration are documented.

## 18. Implementation Sequence Summary

| Phase | Primary output | Must prove before moving on |
|---|---|---|
| 0. Foundation | Runnable services, schema, storage and contracts | Repeatable environment and migrations |
| 1. Extraction | Pages, chunks and preserved images | Correct content and page provenance |
| 2. Retrieval | Standalone semantic-search API | Measured retrieval relevance and latency |
| 3. Augmentation | Inspectable evidence packs | Deterministic context and citation mapping |
| 4. Generation | PydanticAI grounded and supplemented answers | Citation enforcement and graceful provider failure |
| 5. Hardening | Reproducible MVP | End-to-end reliability and documentation |

This ordering should remain fixed unless extraction or retrieval evaluation reveals a foundational issue. Generation should not start merely because an LLM provider is easy to call.
